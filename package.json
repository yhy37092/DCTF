{
  "name": "DCTF",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "ganache-cli": "ganache-cli -m 'unable off subject boil blur state suspect bleak book sentence opera pet'",
    "migrate": "truffle migrate --reset --network develop",
    "test": "echo \"Error: no test specified\" "
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "@openzeppelin/test-helpers": "^0.5.15"
  }
}
